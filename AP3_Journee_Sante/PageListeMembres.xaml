<Page x:Class="AP3_Journee_Sante.PageListeMembres"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageListeMembres">

    <Grid Background="white">

        <!-- LISTVIEW IDENTIQUE À PageAsso -->
        <ListView x:Name="lvmembres"
                  AlternationCount="2"
                  Margin="69,146,69,10"
                  SelectionChanged="lvmembres_SelectionChanged"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto">

            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListViewItem}">
                    <Setter Property="Background" Value="White"/>
                    <Style.Triggers>
                        <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                            <Setter Property="Background" Value="LightGray"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.View>
                <GridView AllowsColumnReorder="true" ColumnHeaderToolTip="Informations Membre">

                    <GridViewColumn Header="Nom" Width="150"
                                    DisplayMemberBinding="{Binding Nom}"/>

                    <GridViewColumn Header="Prénom" Width="150"
                                    DisplayMemberBinding="{Binding Prenom}"/>

                    <GridViewColumn Header="Fonction" Width="150"
                                    DisplayMemberBinding="{Binding Fonction}"/>

                    <GridViewColumn Header="Association" Width="200"
                                    DisplayMemberBinding="{Binding NomAssociation}"/>

                </GridView>
            </ListView.View>
        </ListView>

        <!-- BOUTONS IDENTIQUES À PageAsso -->
        <Button x:Name="btnAjouter"
                Content="Ajouter un membre"
                HorizontalAlignment="Left"
                Margin="50,21,0,0"
                VerticalAlignment="Top"
                Height="46" Width="150"
                Click="btnAjouter_Click"/>

        <Button x:Name="btnModifier"
                Content="Modifier le membre"
                HorizontalAlignment="Left"
                Margin="250,20,0,0"
                VerticalAlignment="Top"
                Height="46" Width="150"
                Click="btnModifier_Click"
                Visibility="Collapsed"/>

        <Button x:Name="btnSupprimer"
                Content="Supprimer le membre"
                HorizontalAlignment="Left"
                Margin="450,20,50,0"
                VerticalAlignment="Top"
                Height="46" Width="150"
                Background="Red" Foreground="White"
                Click="btnSupprimer_Click"
                Visibility="Collapsed"/>


    </Grid>
</Page>
